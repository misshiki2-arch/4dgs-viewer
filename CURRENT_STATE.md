# Current Project State

The canonical project-state record is [`docs/phase3_current_state.md`](docs/phase3_current_state.md).

At the recorded baseline, Phase 2 is closed and Phase 3 Step118 is complete at acceptance Level 3: the native WebGPU production data path reaches final canvas presentation. Step119's `[0, 524288)` zero-visible control is accepted through Impl4-Impl6 and Validation1, including canonical zero-work completion, production-black presentation, and opt-in capture. A CUDA-visible control and matching WebGPU resident workset use source range `[524288, 1048576)` with camera `000151_v13`, frame `151`, time `23.2`, and 1280 x 720 top-left/y-down output.

Investigation6 Fix2 remains accepted: the common presentation/capture orientation is canonical top-left/y-down with no vertical flip, and browser/PNG orientation agrees with CUDA/ground truth under the fixed visible condition. Validation2 is also accepted as a completed comparison operation, not as visual parity: its RGB comparison reports MAE `8.168661747685185`, RMSE `32.02683437269976`, and a different-pixel ratio of `0.19002061631944445` (about 19.002%), classified as `comparison-ready-difference-unclassified`. Normal orientation has lower RMSE than a vertical flip (`32.02683437269976` versus `45.58795176589415`), so Y inversion is not the remaining difference.

Investigation7 has completed the read-only first-mismatch classification. Population/source identity, temporal inputs, normalized left/right quaternion inputs, 4D temporal covariance coupling, and same-index raw screen center agree before the first mismatch. The first semantic mismatch is the production-footprint boundary that should derive conditional 3D covariance from the 4D Gaussian: CUDA consumes both quaternions and XYZT scale and evaluates `Sigma11 - Sigma12 Sigma12^T / Sigma_tt`, while the current WebGPU footprint uses only the left quaternion and XYZ scale. CUDA-formula reconstruction matches all eight direct CUDA covariance records within `1e-5`; the current WebGPU formula exceeds that tolerance for all eight. The WebGPU conditional world covariance is not stored as direct GPU intermediate evidence in the current artifact and was reconstructed from captured production inputs and the current WGSL, so that provenance boundary remains explicit. Camera-space covariance and all later conic, radius/footprint, opacity/SH color, tile, sort, and compositor stages remain unclassified rather than presumed correct.

The previously suspected compact-expectation/runtime inconsistency is also classified. `captureExpectationContract.canonicalExecution.ready` contains the zero-reference-specific `genericCanonicalZeroExecutionReady` predicate. Its `false` value is expected for the Fix2 `nonzero-reference` capture and does not contradict runtime `executionCompletionReady: true`; the complete capture expectation is `readyBeforePng: true`, `ready: true`, with no blocked reason. Step119 therefore closes as fixed-population semantic control and first-mismatch classification, not as fixed-range visual parity or acceptance Level 4. Step120 is limited to connecting CUDA-aligned conditional 4D-to-3D covariance to the production footprint. After that change, downstream comparison resumes and stops at the next first mismatch without presuming visual-parity success.

The 524,288-record controls establish only fixed-resident-range semantics, not full-scene parity. After fixed-range semantic parity is closed, complete handling of all 3,231,588 source records by full residency or correctness-preserving chunk/streaming behavior, followed by a matched full-scene CUDA comparison, is a mandatory gate before final visual-parity or production-acceptance claims. Interactive production acceptance, performance, and LOD remain unclaimed and require their own later acceptance boundaries.

Before selecting or implementing a new Step, read the canonical record and obtain an explicitly reviewed Step objective. Historical `Current`, `Next`, `current goal`, and future-work text elsewhere in the repository does not independently authorize implementation.
