# Current Project State

The canonical project-state record is [`docs/phase3_current_state.md`](docs/phase3_current_state.md).

At the recorded baseline, Phase 2 is closed and Phase 3 Step118 is complete at acceptance Level 3: the native WebGPU production data path reaches final canvas presentation. Step119 then established zero-visible and CUDA-visible fixed-population controls and canonical top-left/y-down presentation. Its visible control uses source range `[524288, 1048576)`, 524,288 records, camera `000151_v13`, frame 151, time 23.2, and 1280 x 720 output. The accepted RGB comparison remained a classified difference rather than visual parity.

Step120 is now complete through Impl1, Impl2, and Impl2 Fix1-Fix4. Production footprint covariance uses the CUDA-aligned conditional 4D-to-3D form `Sigma11 - Sigma12 Sigma12^T / Sigma_tt`, built from both normalized quaternions and XYZT scale. Temporal mean and footprint therefore share the same 4D covariance vocabulary. Camera transform, projection Jacobian, orientation, conic/radius downstream formulas, opacity/SH, tile list, sort, compositor, scheduler, presentation ownership, and capture lifecycle were not changed by Step120.

The accepted Fix4 browser artifact carries eight valid canonical Step113 representatives at source indices `658947`, `771007`, `788034`, `826401`, `835183`, `852955`, `863505`, and `906711`. All map authoritatively to evaluator rows `0..7`, use `sourceCode: 113`, have `firstMismatchStage: none`, and confirm the conditional world covariance through camera covariance, Jacobian, screen covariance, conic, and radius for those eight records. The largest recorded absolute errors are `7.315222377846098e-8`, `5.944491415776909e-8`, `2.4726137084485345e-6`, `3.6210082640764085e-5`, `8.152026688135194e-7`, and `0`, respectively.

The generic fixed-record comparison processed 65,536 candidate/computed/compared rows, with eight valid actual/expected rows, `fieldMismatchCount: 0`, `mismatchClassification: none`, and maximum absolute error `9.1552734375e-5` under tolerance `1e-3`. This does not describe the whole resident range: the diagnostic population prepends the canonical eight and then fills with deduplicated CPU fallback indices `0..65527`. It is not the resident interval `[524288, 1048576)`, and the result does not prove that only eight of 524,288 records are visible or that every remaining resident record is correctly culled.

Fix4 preserved the browser/runtime baseline: the saved 1280 x 720 PNG is fresh and nonblank, its Blob/saved-file identities agree, and its SHA-256 is `f843845d62e2377352cd998b3a09a5a15da4afdc60c23498c51795f2e594fec0`, unchanged from Fix3. Design C's `diagnosticExecutionDecision`, `captureArtifactDecision`, `detailDecision`, and overall `decision` are `ready`, `comparisonDecision` is `match`, and `blockedReasons` is empty. The legacy `step113CovarianceJacobianConicParity` view remains blocked because it expects the historical Step113 phase/root; this is a Summary-consumer synchronization issue, not a Step120 runtime failure.

Step120 closes the conditional-covariance production change and the bounded evidence listed above. It does not prove all stages for all 524,288 resident records, fixed-range visual parity, acceptance Level 4, opacity/SH, tile coverage, sorting, compositor accumulation, full-scene correctness for all 3,231,588 records, interactive acceptance, performance, scalability, LOD, streaming, or final production acceptance. A future read-only investigation may design a population-aligned comparison for all 524,288 resident records, but no Step121 implementation is authorized by this document.

Before selecting or implementing a new Step, read the canonical record and obtain an explicitly reviewed Step objective. Historical `Current`, `Next`, `current goal`, and future-work text elsewhere in the repository does not independently authorize implementation.
