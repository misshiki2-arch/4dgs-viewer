# Current Project State

The canonical project-state record is [`docs/phase3_current_state.md`](docs/phase3_current_state.md).

Phase 2 is closed and Phase 3 Step118 remains complete at acceptance Level 3: the native WebGPU production data path reaches final canvas presentation. Step119 established zero-visible and CUDA-visible fixed-population controls and canonical top-left/y-down presentation. Its visible control uses source range `[524288, 1048576)`, 524,288 records, camera `000151_v13`, frame 151, time 23.2, and 1280 x 720 output. The accepted RGB comparison remained a classified difference rather than visual parity.

Step120 is complete through Impl1, Impl2, and Impl2 Fix1-Fix4. Production footprint covariance uses the CUDA-aligned conditional 4D-to-3D form `Sigma11 - Sigma12 Sigma12^T / Sigma_tt`, built from both normalized quaternions and XYZT scale. Temporal mean and footprint therefore share the same 4D covariance vocabulary. Step120's bounded canonical-eight and generic fixed-record evidence remains historical support for that change; it was not a comparison of the complete resident interval.

Step121 is complete in the limited record-local semantic-parity scope defined for the fixed resident range. A bounded single-chunk producer and sequential eight-chunk orchestrator process all 524,288 source records as 8 x 65,536 records on one fresh caller-owned diagnostic device. A completed production frame publishes the bounded input identity used by the one-shot controller; the diagnostic device is separate from the production device, and production computation does not depend on diagnostic readback.

The accepted Fix6 browser artifact `phase3_step121_impl2_fix6_000151_v13_population_semantic_comparison.json` has SHA-256 `db827a6741e4111f57a9469f2dd20535fdbdcee696fe459dd926017899d88a33`. Controller and orchestration decisions are `match`; all 8 chunks and 524,288 unique source indices complete with no missing, invalid, duplicate, out-of-range, ordering, gap, or overlap evidence. The defined 13 stages from temporal eligibility through current expected-oracle inclusive tile bounds have zero semantic residual. Four pixel-boundary records (`803621`, `817028`, `820164`, and `833130`) retain four raw component differences classified as `precision-aligned`; their semantic residual is zero. The common center/radius tile-bounds formula removes the Fix5 internal-oracle residual of 5,079 records / 5,434 components, leaving tile raw and semantic residual counts at zero. Investigation10 limits this result to WebGPU actual versus the current expected oracle; it does not establish CUDA-direct Jacobian, screen covariance, conic, radius, tile-bounds, or tile-reference parity.

User browser observation after Fix6 reported a normal, stable, nonblank Viewer with no obvious coverage holes, black omissions, or flicker. Fix6 did not create a new PNG, so this acceptance does not establish byte identity with the Fix5 PNG or quantitative parity with the CUDA Reference PNG.

Step121 does not prove bitwise equality, fixed-range PNG visual parity, opacity/SH/color parity, tile-reference population/count parity, depth-key or sort parity, ordered per-tile reference-list parity, compositor accumulation parity, final RGB parity, acceptance Level 4, correctness for all 3,231,588 source records, interactive camera/time acceptance, performance/scalability, LOD/streaming, or final production acceptance.

Step122 has progressed through Impl1 Fix3 and its expected-only browser gate, Impl1 Fix4 diagnostic RGB/alpha validity separation, and Step122 Impl3 production degree-2 spatial SH implementation, automatic validation, and fixed-range browser semantic acceptance. Step122 as a whole is not complete. The earlier accepted Impl1 Fix2 artifact remains the historical baseline at 21,012 RGB-mismatch records / 47,469 components with maximum error `0.9877071223567659`. Fix3 changed only the comparison expected oracle's degree-2 C2 signs and preserved production source, output schema/provenance vocabulary, and expected/actual independence.

The accepted Fix3 artifact `phase3_step122_impl1_fix3_000151_v13_population_semantic_comparison.json` has SHA-256 `af8056194704fe15cf48de08a042c0221f971f2d960c352b6fea280c8176f639` and covers all 524,288 records in `[524288, 1048576)` as 8 x 65,536 chunks. Coverage is complete, all 15 stages have zero missing/invalid evidence, the upstream 13-stage semantic residual remains zero, and alpha remains 0 record/component mismatches for all 21,818 eligible records. The corrected current RGB baseline is 21,030 records / 47,529 components with maximum error `0.9841542530436316`, first at srcIndex 659525 / R; `productionTileInputRgb` remains the first semantic/downstream mismatch. User browser observation was normal. Fix3 did not change production source or save a PNG, so it establishes neither production RGB nor PNG visual parity.

Investigation8 found an independent diagnostic-consumer defect before production spatial SH. Step122 Impl1 Fix4 has now removed the shared `colorAlphaValid`: RGB validity is three finite, nonnegative components with no upper bound, while alpha validity is independently finite and in `(0, 1]`. Focused fixtures cover RGB above 1 as valid match/mismatch evidence and fail-closed negative/nonfinite RGB or invalid alpha without cross-stage invalidation. The 52-case single-chunk smoke, eight-chunk orchestration, controller regression, syntax checks, build, and diff checks passed. Fix4 changed neither production RGB semantics nor schema/provenance/stage vocabulary.

Step122 Impl3 replaced the production evaluator's historical `clamp(f_dc + 0.5, 0, 1)` RGB path with CUDA-aligned degree-2 spatial SH. It uses coefficient-major `sh[0]` through `sh[8]`, C2 signs `[+, -, +, -, +]`, original Gaussian position minus the row-major-view camera center, `+0.5`, and a lower-only clamp. The versioned candidate input contract `phase3-step122-production-candidate-attribute-input-layout-v1` uses binding 4 without adding storage bindings and packs 32 floats / 8 vec4 / 128 bytes per candidate. The Gaussian attribute contract is `phase3-step122-webgpu-gaussian-attribute-evaluation-contract-v2`. Higher spatial degree and temporal SH remain deferred.

The browser acceptance bundle `phase3_step122_impl3_000151_v13_browser_acceptance.json` is 55,471 bytes with SHA-256 `3dd4706918191c46727dbf54d68c201524a6e28e8f690bed9cc24ce990282e4b`. On the fixed range `[524288, 1048576)`, all 8 chunks and 524,288 unique records completed with no coverage or missing/invalid evidence. Within the same WebGPU-actual/current-expected contract, the upstream 13-stage semantic residual is zero. All 21,818 eligible records match for alpha and RGB: alpha maximum absolute error is `2.682209014892578e-7`, RGB maximum absolute error is `3.3570440627350706e-7`, both below tolerance `1e-5`; first semantic/downstream mismatch is null. The fresh diagnostic device was acquired and destroyed successfully, and user browser observation reported no visible problem.

The capture-side aggregate reported `accepted:false` because the WebGPU-exclusive lifecycle wrapper did not directly serialize the Gaussian attribute contract version, evaluation mode, or candidate layout. Its `webgpu-exclusive-frame-lifecycle-pending` status records that WebGPU owns the Viewer canvas lifecycle and suppresses the WebGL2 frame; it is not a production semantic failure. `nativeProductionFrameDataPathReady` is true and the fifteen-stage comparison is `match`. Direct serialized production-layout evidence is therefore inconclusive in this bundle. The exact layout remains established by current source, focused smoke, automatic validation, and the ready production data path; no capture-only publication Fix is required.

The canonical local Python environment for CUDA Reference generation and validation, related focused tests, fixed-condition PNG comparison, and pixel-evidence generation is conda environment `4dgs310`, using `/home/demo/miniconda3/envs/4dgs310/bin/python`. Relevant Step122-or-later procedures must reuse this environment without assuming shell activation. The fixed CUDA baseline is bound to this canonical environment together with the render-state manifest, asset identity, camera, time, range, and generated artifact; arbitrary CUDA hardware, driver, compiler, or `__expf` behavior is not the WebGPU portability contract.

Step122 Validation2 completed the post-Impl3 fresh fixed-condition PNG comparison. The WebGPU PNG is fresh, current, nonblank, 1280 x 720, and tied to the requested/presented production generation; runtime health and capture lifecycle are ready. Against the CUDA PNG generated under the then-current split camera/raster contract, the measured metrics are MAE `6.42220630787037`, RMSE `27.03804631164564`, maximum absolute error `254`, and 153,894 different pixels (`0.16698567708333334`); the WebGPU production reference count is 229,328. Relative to Validation1, MAE improved by about 12.6% and RMSE by about 9.0%, while different pixels increased by 906 and the WebGPU nonblack population increased by 1,179. Normal orientation has lower error than a vertical flip. This remains a valid historical measurement of those two generated PNGs, but it is not a final parity baseline for a coherent CUDA camera/raster contract.

Investigation10 found a split inside the canonical CUDA Reference camera contract. Intrinsics cameras enter with raw `FoVx = FoVy = -1` and valid `fl_x/fl_y/cx/cy`; full projection uses the valid focal intrinsics, while rasterizer footprint settings used `tanFovX = tanFovY = -0.5463024974`, `focalX = -1171.512085`, and `focalY = -658.975586` derived from the sentinel. The manifest instead publishes positive effective values (`fx = fy = 1777.7777777777778`, `tanFovX = 0.36`, `tanFovY = 0.2025`). The canonical eight records therefore match through conditional covariance, unclamped camera position, depth, camera covariance, and projected center, but first diverge at camera-position clamping; in the published fifteen-stage vocabulary the first mismatch is Jacobian `j00` for srcIndex 658947 (CUDA `-13.801024436950684`, current expected `20.943151606426603`). The mismatch propagates through screen covariance, conic, radius, tile rect, and `tilesTouched`, so the current 13/15-stage gates prove internal WebGPU actual/current-expected consistency rather than CUDA-direct screen/raster parity. The cause classification is `mixed-divergence`, not an f32 or WebGPU-production-only difference.

Investigation11 classifies the training provenance formally as `D: training-provenance-insufficient`, while the source and artifact lineage strongly indicate that the legacy checkpoint was optimized through the historical split camera/raster semantics. The user therefore selected a corrected baseline rather than implementing those historical negative-sentinel footprint semantics in the Viewer. Viewer development is frozen partway through Step122. The shared training/render camera handoff defect must be corrected and validated, followed by from-scratch retraining; this documentation sync does not start either task.

The current checkpoint, SPL4, CUDA Reference, Viewer JSON/PNG, and Step119-122 acceptance measurements remain immutable legacy historical evidence. They must not be deleted, renamed, overwritten, or reused as corrected output locations. Step118-122 production, diagnostic, comparison, capture, and PNG infrastructure remains completed and reusable, but its asset-specific semantic counts, fixed range `[524288, 1048576)`, 3,231,588-record population, 229,328 production reference count, and PNG metrics are not acceptance values for the corrected baseline.

The main dependency track is now: make user-owned Git checkpoints; fix the shared CUDA/training renderer intrinsics handoff while preserving legacy FoV-only cameras; run focused validation and publish raw/effective rasterizer intrinsics; use the legacy checkpoint only for a diagnostic corrected-renderer comparison; retrain from scratch into a new non-overwriting identity; export a new SPL4; generate a corrected CUDA Reference; and establish the new population provenance, record count, camera, time, representative records, and fixed range. The candidate retraining output is `/home/demo/work/outputs/sph_scene_4dgs_corrected_intrinsics_v1`, but it has not been created or authorized by this documentation task.

The Viewer may be unfrozen only after the corrected handoff is reviewed, focused validation is accepted, from-scratch retraining is complete, the new checkpoint/SPL4/CUDA Reference identities are fixed, and a new population/fixed-range comparison baseline is designed. Corrected fixed-range semantic, tile-reference, sort, compositor, and PNG parity must then be rerun before the corrected full-scene gate. Step122 overall, Acceptance Level 4, full-scene correctness, interactive acceptance, performance/scalability, and LOD/streaming remain incomplete.
