import {
  createProgram,
  createArrayBuffer,
  bindFloatAttrib,
  clearToGray,
  enableStandardAlphaBlend,
  disableDepth
} from './gpu_gl_utils.js';
import { GPU_STEP_VERTEX_SHADER, GPU_STEP_FRAGMENT_SHADER } from './gpu_shaders.js';
import { computeTileGrid, buildTileLists, summarizeTileLists } from './gpu_tile_utils.js';
import { buildTileHeatmapImageData, drawTileHeatmapOverlay, formatTileDebugSummary, getTilePixelRect } from './gpu_tile_debug.js';
import { getDrawTileMode, setDefaultDrawTileMode, chooseFocusTileId, buildNeighborTileIds, buildPerTileDrawIndexLists, summarizeTileDrawBatches, formatTileSelectionState } from './gpu_tile_select.js';
import { buildVisibleSplats, getVisibleBuildConfig } from './gpu_visible_builder.js';
import { buildDrawArraysFromIndices, buildPerTileDrawBatches, summarizePerTileDrawBatches, uploadAndDraw, renderPerTileBatches, buildDrawStats } from './gpu_draw_utils.js';
import { uploadAndDrawPackedDirect, summarizePackedDirectResources } from './gpu_packed_draw_executor.js';
import { formatGpuViewerInfo, setInfoText } from './gpu_info_utils.js';
import { buildGpuDebugExtraLines } from './gpu_debug_info_builder.js';
import { getRequestedDrawPath, resolveDrawPath, summarizeDrawPathSelection, GPU_DRAW_PATH_LEGACY, GPU_DRAW_PATH_PACKED } from './gpu_draw_path_selector.js';

function ensureDebugOverlayCanvas(mainCanvas){ let overlay=document.getElementById('gpuTileDebugOverlay'); if(!overlay){ overlay=document.createElement('canvas'); overlay.id='gpuTileDebugOverlay'; Object.assign(overlay.style,{position:'absolute',inset:'0',width:'100%',height:'100%',pointerEvents:'none',zIndex:'2',display:'none'}); const parent=mainCanvas.parentElement||document.body; parent.appendChild(overlay);} return overlay; }
function applyUiTileModeToGlobals(ui){ if(ui.showTileDebugCheck) window.__GPU_TILE_DEBUG_OVERLAY__=!!ui.showTileDebugCheck.checked; if(ui.drawSelectedTileOnlyCheck) window.__GPU_TILE_DRAW_SELECTED_ONLY__=!!ui.drawSelectedTileOnlyCheck.checked; if(ui.useMaxTileCheck) window.__GPU_TILE_USE_MAX_TILE__=!!ui.useMaxTileCheck.checked; if(ui.selectedTileIdInput){ const v=Number(ui.selectedTileIdInput.value); window.__GPU_TILE_SELECTED_ID__=Number.isInteger(v)?v:-1; } if(ui.tileRadiusInput){ const r=Number(ui.tileRadiusInput.value); window.__GPU_TILE_RADIUS__=Number.isInteger(r)&&r>=0?r:0; } }
function buildFocusTileRects(tileIds,tileGrid,canvasWidth,canvasHeight){ const rects=[]; for(const tileId of tileIds){ const tx=tileId%tileGrid.tileCols; const ty=Math.floor(tileId/tileGrid.tileCols); rects.push({tileId,tx,ty,rect:getTilePixelRect(tx,ty,tileGrid.tileSize,canvasWidth,canvasHeight)});} return rects; }
function enableTileScissor(gl,canvas,tileRect){ const [x0,y0,x1,y1]=tileRect; const w=Math.max(0,x1-x0), h=Math.max(0,y1-y0), scY=canvas.height-y1; gl.enable(gl.SCISSOR_TEST); gl.scissor(x0,scY,w,h);} function disableTileScissor(gl){ gl.disable(gl.SCISSOR_TEST); }
function buildAllVisibleDrawData(visible){ const allDrawIndices=new Uint32Array(visible.length); for(let i=0;i<visible.length;i++) allDrawIndices[i]=i; return buildDrawArraysFromIndices(visible, allDrawIndices); }
function fmtNum(v,digits=4){ return Number.isFinite(v)?Number(v).toFixed(digits):'NaN'; }
function fmtArr(arr,digits=4){ if(!Array.isArray(arr)) return 'none'; return '['+arr.map((v)=>fmtNum(v,digits)).join(', ')+']'; }
function buildLegacySample(visible){ if(!Array.isArray(visible)||visible.length===0) return null; const v=visible[0]; return { centerPx:[v.px,v.py], radiusPx:v.radius, colorAlpha:Array.isArray(v.colorAlpha)?v.colorAlpha.slice(0,4):null, conic:Array.isArray(v.conic)?v.conic.slice(0,3):null }; }
function buildPackedSample(packedScreenSpace){ const packed=packedScreenSpace?.packed; if(!(packed instanceof Float32Array)||packed.length<16) return null; return { centerPx:[packed[0],packed[1]], radiusPx:packed[2], depth:packed[3], colorAlpha:[packed[4],packed[5],packed[6],packed[7]], conic:[packed[8],packed[9],packed[10]], reserved:packed[11], misc:[packed[12],packed[13],packed[14],packed[15]] }; }
function buildEffectiveDrawPathSelection({mode,requestedPath,packedScreenSpace}){ const base=resolveDrawPath({requestedPath,hasPackedScreenSpace:!!packedScreenSpace?.packed,hasGpuScreenPath:false}); const summary=summarizeDrawPathSelection(base); if(!mode.drawSelectedOnly) return {...summary, forceLegacyPerTile:false}; return {...summary, actualPath:GPU_DRAW_PATH_LEGACY, fallbackReason:summary.actualPath===GPU_DRAW_PATH_LEGACY?summary.fallbackReason:'per-tile-legacy-only', forceLegacyPerTile:true}; }
function executeFullFrameDraw({gl,gpu,canvas,drawPathSelection,packedScreenSpace,legacyDrawData}){ if(drawPathSelection.actualPath===GPU_DRAW_PATH_PACKED){ const directPackedDrawInfo=uploadAndDrawPackedDirect(gl,gpu,packedScreenSpace,canvas.width,canvas.height); const packedUploadSummary={...(directPackedDrawInfo.uploadSummary||{}), packedDirectDraw:!!directPackedDrawInfo.packedDirectDraw, packedDirectLayoutVersion:directPackedDrawInfo.packedDirectLayoutVersion??0, packedDirectStrideBytes:directPackedDrawInfo.packedDirectStrideBytes??0, packedDirectAttributeCount:directPackedDrawInfo.packedDirectAttributeCount??0, packedDirectOffsets:directPackedDrawInfo.packedDirectOffsets??'', packedInterleavedBound:!!directPackedDrawInfo.packedInterleavedBound, legacyExpandedArraysBuilt:false}; return { executionSummary:{ tileBatchCount:1, nonEmptyTileBatchCount:0, totalTileDrawCount:directPackedDrawInfo.drawCount??0, uploadCount:1, drawCallCount:1, requestedDrawPath:drawPathSelection.requestedPath, actualDrawPath:drawPathSelection.actualPath, drawPathFallbackReason:drawPathSelection.fallbackReason }, packedUploadSummary, directPackedDrawInfo}; } uploadAndDraw(gl,gpu,legacyDrawData,canvas.width,canvas.height); return { executionSummary:{ tileBatchCount:1, nonEmptyTileBatchCount:1, totalTileDrawCount:legacyDrawData?.nDraw??0, uploadCount:1, drawCallCount:1, requestedDrawPath:drawPathSelection.requestedPath, actualDrawPath:drawPathSelection.actualPath, drawPathFallbackReason:drawPathSelection.fallbackReason }, packedUploadSummary:{ packedDirectDraw:false, packedDirectLayoutVersion:0, packedDirectStrideBytes:0, packedDirectAttributeCount:0, packedDirectOffsets:'', packedInterleavedBound:false, legacyExpandedArraysBuilt:true }, directPackedDrawInfo:null }; }
function executePerTileLegacyDraw({gl,gpu,canvas,focusTileRects,perTileDrawBatches,drawPathSelection}){ const rectMap=new Map(focusTileRects.map((item)=>[item.tileId,item.rect])); const executionSummary=renderPerTileBatches(gl,gpu,perTileDrawBatches,canvas.width,canvas.height,{ beforeTile:(item)=>{ const rect=rectMap.get(item.tileId); if(rect) enableTileScissor(gl,canvas,rect); else disableTileScissor(gl); }, afterTile:()=>{ disableTileScissor(gl); } }); disableTileScissor(gl); executionSummary.requestedDrawPath=drawPathSelection.requestedPath; executionSummary.actualDrawPath=GPU_DRAW_PATH_LEGACY; executionSummary.drawPathFallbackReason=drawPathSelection.fallbackReason; return { executionSummary, packedUploadSummary:{ packedDirectDraw:false, packedDirectLayoutVersion:0, packedDirectStrideBytes:0, packedDirectAttributeCount:0, packedDirectOffsets:'', packedInterleavedBound:false, legacyExpandedArraysBuilt:true }, directPackedDrawInfo:null }; }
function buildPackedLines(buildStats,drawPathSelection,drawStats){ return [`packedVisiblePathEnabled=${!!buildStats?.packedVisiblePathEnabled}`,`packedVisiblePathUsed=${!!buildStats?.packedVisiblePathUsed}`,`packedVisiblePath=${buildStats?.packedVisiblePath??'none'}`,`packedVisibleCount=${buildStats?.packedVisibleCount??0}`,`packedVisibleLength=${buildStats?.packedVisibleLength??0}`,`packedVisibleFloatsPerItem=${buildStats?.packedVisibleFloatsPerItem??0}`,`packedAlphaSource=colorAlpha[3]`,`requestedDrawPath=${drawPathSelection.requestedPath}`,`actualDrawPath=${drawPathSelection.actualPath}`,`drawPathFallbackReason=${drawPathSelection.fallbackReason}`,`packedUploadBytes=${drawStats?.packedUploadBytes??0}`,`packedUploadCount=${drawStats?.packedUploadCount??0}`,`packedUploadLength=${drawStats?.packedUploadLength??0}`,`packedUploadCapacityBytes=${drawStats?.packedUploadCapacityBytes??0}`,`packedUploadReusedCapacity=${!!drawStats?.packedUploadReusedCapacity}`,`packedDirectDraw=${!!drawStats?.packedDirectDraw}`,`packedDirectLayoutVersion=${drawStats?.packedDirectLayoutVersion??0}`,`packedDirectStrideBytes=${drawStats?.packedDirectStrideBytes??0}`,`packedDirectAttributeCount=${drawStats?.packedDirectAttributeCount??0}`,`packedDirectOffsets=${drawStats?.packedDirectOffsets??''}`,`packedInterleavedBound=${!!drawStats?.packedInterleavedBound}`,`legacyExpandedArraysBuilt=${!!drawStats?.legacyExpandedArraysBuilt}`]; }
function buildSampleLines(legacySample,packedSample){ return [`legacySampleCenterPx=${legacySample?fmtArr(legacySample.centerPx,3):'none'}  packedSampleCenterPx=${packedSample?fmtArr(packedSample.centerPx,3):'none'}`,`legacySampleRadiusPx=${legacySample?fmtNum(legacySample.radiusPx,3):'none'}  packedSampleRadiusPx=${packedSample?fmtNum(packedSample.radiusPx,3):'none'}`,`legacySampleColorAlpha=${legacySample?fmtArr(legacySample.colorAlpha,4):'none'}`,`packedSampleColorAlpha=${packedSample?fmtArr(packedSample.colorAlpha,4):'none'}`,`legacySampleConic=${legacySample?fmtArr(legacySample.conic,4):'none'}`,`packedSampleConic=${packedSample?fmtArr(packedSample.conic,4):'none'}`,`packedSampleReserved=${packedSample?fmtNum(packedSample.reserved,4):'none'}`,`packedSampleMisc=${packedSample?fmtArr(packedSample.misc,2):'none'}`]; }
function computeDrawFraction(visibleCount,drawCount){ if(!Number.isFinite(visibleCount)||visibleCount<=0) return 0; if(!Number.isFinite(drawCount)) return 0; return drawCount/visibleCount; }
function buildSafeBuildStats(rawBuildStats,visible,packedScreenSpace,elapsedMs){ const visibleCount=Array.isArray(visible)?visible.length:0; const packed=packedScreenSpace?.packed; return { ...(rawBuildStats||{}), packedVisiblePathEnabled:!!rawBuildStats?.packedVisiblePathEnabled, packedVisiblePathUsed:!!rawBuildStats?.packedVisiblePathUsed, packedVisiblePath:rawBuildStats?.packedVisiblePath??packedScreenSpace?.path??'none', packedVisibleCount:Number.isFinite(rawBuildStats?.packedVisibleCount)?rawBuildStats.packedVisibleCount:(Number.isFinite(packedScreenSpace?.packedCount)?packedScreenSpace.packedCount:visibleCount), packedVisibleLength:Number.isFinite(rawBuildStats?.packedVisibleLength)?rawBuildStats.packedVisibleLength:(packed instanceof Float32Array?packed.length:0), packedVisibleFloatsPerItem:Number.isFinite(rawBuildStats?.packedVisibleFloatsPerItem)?rawBuildStats.packedVisibleFloatsPerItem:(Number.isFinite(packedScreenSpace?.floatsPerItem)?packedScreenSpace.floatsPerItem:0), visibleBuildMs:Number.isFinite(rawBuildStats?.visibleBuildMs)?rawBuildStats.visibleBuildMs:elapsedMs, candidateBuildMs:Number.isFinite(rawBuildStats?.candidateBuildMs)?rawBuildStats.candidateBuildMs:0, screenSpaceBuildMs:Number.isFinite(rawBuildStats?.screenSpaceBuildMs)?rawBuildStats.screenSpaceBuildMs:0 }; }
export function createGpuRenderer(canvas){ const gl=canvas.getContext('webgl2',{alpha:false,antialias:false,premultipliedAlpha:false,preserveDrawingBuffer:false}); if(!gl) throw new Error('WebGL2 is not available in this browser.'); const program=createProgram(gl,GPU_STEP_VERTEX_SHADER,GPU_STEP_FRAGMENT_SHADER); const emptyF32=new Float32Array(0); const centerBuffer=createArrayBuffer(gl,emptyF32), radiusBuffer=createArrayBuffer(gl,emptyF32), colorBuffer=createArrayBuffer(gl,emptyF32), conicBuffer=createArrayBuffer(gl,emptyF32); const vao=gl.createVertexArray(); bindFloatAttrib(gl,{vao,program,buffer:centerBuffer,name:'aCenterPx',size:2}); bindFloatAttrib(gl,{vao,program,buffer:radiusBuffer,name:'aRadiusPx',size:1}); bindFloatAttrib(gl,{vao,program,buffer:colorBuffer,name:'aColorAlpha',size:4}); bindFloatAttrib(gl,{vao,program,buffer:conicBuffer,name:'aConic',size:3}); const uViewportPx=gl.getUniformLocation(program,'uViewportPx'); const renderer={gl,program,vao,centerBuffer,radiusBuffer,colorBuffer,conicBuffer,uViewportPx,width:canvas.width,height:canvas.height,resize(width,height){ this.width=width; this.height=height; gl.viewport(0,0,width,height);} }; renderer.resize(canvas.width,canvas.height); return renderer; }
export async function renderGpuFrame({raw,gpu,canvas,camera,controls,ui,tokenRef,infoEl,interactionOverride=null}){ const gl=gpu.gl; const bg255=parseInt(ui.bgGraySlider.value,10); const bg=bg255/255.0; controls.update(); camera.updateMatrixWorld(true); setDefaultDrawTileMode(window); applyUiTileModeToGlobals(ui); const mode=getDrawTileMode(window,ui); const debugOverlayCanvas=ensureDebugOverlayCanvas(canvas); const debugCtx=debugOverlayCanvas.getContext('2d'); if(!raw){ gpu.resize(canvas.width,canvas.height); gl.disable(gl.BLEND); clearToGray(gl,bg); debugOverlayCanvas.width=canvas.width; debugOverlayCanvas.height=canvas.height; debugCtx.clearRect(0,0,debugOverlayCanvas.width,debugOverlayCanvas.height); debugOverlayCanvas.style.display='none'; const emptyInfo='GPU Step25 viewer\nNo scene loaded.'; setInfoText(infoEl,emptyInfo); return {infoText:emptyInfo,visible:[],packedScreenSpace:null,packedSummary:null,buildStats:null,drawStats:null}; }
 const frameToken=++tokenRef.value; const t0=performance.now(); const buildConfig=getVisibleBuildConfig(ui,interactionOverride); const tileGrid=computeTileGrid(canvas.width,canvas.height,32); const camPos=camera.position.clone(); const visibleResult=await buildVisibleSplats({raw,camera,canvasWidth:canvas.width,canvasHeight:canvas.height,camPos,tokenRef,frameToken,tileGrid,temporalSigmaThreshold:3.0,enablePackedVisiblePath:!!buildConfig.enablePackedVisiblePath,...buildConfig}); if(visibleResult===null) return null; const {visible,packedScreenSpace,packedSummary,activeTileBox,buildStats:rawBuildStats}=visibleResult; const tileData=buildTileLists(visible,tileGrid.tileCols,tileGrid.tileRows); const tileSummary=summarizeTileLists(tileData,tileGrid.tileCols,tileGrid.tileRows,activeTileBox); const legacySample=buildLegacySample(visible); const packedSample=buildPackedSample(packedScreenSpace); const focusTileId=chooseFocusTileId(tileData,mode); const focusTileIds=mode.drawSelectedOnly?buildNeighborTileIds(focusTileId,tileGrid.tileCols,tileGrid.tileRows,mode.tileRadius):[]; const tileBatches=mode.drawSelectedOnly?buildPerTileDrawIndexLists(visible,tileData,focusTileIds,true):[]; const perTileDrawBatches=mode.drawSelectedOnly?buildPerTileDrawBatches(visible,tileBatches):[]; const tileBatchSummary=mode.drawSelectedOnly?summarizeTileDrawBatches(tileBatches):null; const perTileDrawSummary=mode.drawSelectedOnly?summarizePerTileDrawBatches(perTileDrawBatches):null; const effectiveTileSummary=perTileDrawSummary||tileBatchSummary; const focusTileRects=mode.drawSelectedOnly?buildFocusTileRects(focusTileIds,tileGrid,canvas.width,canvas.height):[]; const requestedPath=getRequestedDrawPath(ui); const drawPathSelection=buildEffectiveDrawPathSelection({mode,requestedPath,packedScreenSpace}); const needsLegacyExpandedArrays=drawPathSelection.actualPath===GPU_DRAW_PATH_LEGACY||mode.drawSelectedOnly; const legacyDrawData=needsLegacyExpandedArrays?buildAllVisibleDrawData(visible):null; gpu.resize(canvas.width,canvas.height); disableDepth(gl); enableStandardAlphaBlend(gl); clearToGray(gl,bg); let executionResult; if(mode.drawSelectedOnly){ executionResult=executePerTileLegacyDraw({gl,gpu,canvas,focusTileRects,perTileDrawBatches,drawPathSelection}); } else { executionResult=executeFullFrameDraw({gl,gpu,canvas,drawPathSelection,packedScreenSpace,legacyDrawData}); } const {executionSummary,packedUploadSummary,directPackedDrawInfo}=executionResult; debugOverlayCanvas.width=canvas.width; debugOverlayCanvas.height=canvas.height; debugCtx.clearRect(0,0,debugOverlayCanvas.width,debugOverlayCanvas.height); if(mode.showOverlay){ const heatmap=buildTileHeatmapImageData({tileCounts:tileData.counts,tileCols:tileGrid.tileCols,tileRows:tileGrid.tileRows,tileSize:tileGrid.tileSize,canvasWidth:canvas.width,canvasHeight:canvas.height,highlightTileId:focusTileId,selectedTileIds:mode.drawSelectedOnly?focusTileIds:null,alpha:0.35}); drawTileHeatmapOverlay(debugCtx,heatmap); debugOverlayCanvas.style.display='block'; } else { debugOverlayCanvas.style.display='none'; }
 const elapsed=performance.now()-t0; const buildStats=buildSafeBuildStats(rawBuildStats,visible,packedScreenSpace,elapsed); const avgRefsPerVisible=visible.length>0?(tileSummary.totalRefs/visible.length):0; const tileDebugText=formatTileDebugSummary({tileData,tileCols:tileGrid.tileCols,tileRows:tileGrid.tileRows,tileSize:tileGrid.tileSize,highlightTileId:focusTileId,canvasWidth:canvas.width,canvasHeight:canvas.height}); const tileSelectionText=formatTileSelectionState(mode,focusTileId,focusTileIds,effectiveTileSummary); const effectiveDrawData=mode.drawSelectedOnly?{nDraw:effectiveTileSummary?effectiveTileSummary.totalTileDrawCount:0}:{ nDraw: drawPathSelection.actualPath===GPU_DRAW_PATH_PACKED ? (directPackedDrawInfo?.drawCount??packedScreenSpace?.packedCount??0) : (legacyDrawData?.nDraw??0) }; const packedDirectResourceSummary=summarizePackedDirectResources(gpu); const drawStats=buildDrawStats({visibleCount:visible.length,drawData:effectiveDrawData,mode,focusTileId,focusTileIds,tileBatchSummary:effectiveTileSummary,executionSummary,packedScreenSpace,packedUploadSummary:{...packedUploadSummary,...packedDirectResourceSummary}}); drawStats.drawFraction=computeDrawFraction(drawStats.visibleCount,drawStats.drawCount); const extraLines=buildGpuDebugExtraLines({buildConfig,buildStats,drawStats,mode,focusTileIds,focusTileRects,ui}); const packedLines=buildPackedLines(buildStats,drawPathSelection,drawStats); const sampleLines=buildSampleLines(legacySample,packedSample); const infoText=formatGpuViewerInfo({raw,visibleCount:visible.length,drawCount:effectiveDrawData?.nDraw??0,stride:buildConfig.stride,useRot4d:buildConfig.useRot4d,useSH:buildConfig.useSH,useNativeRot4d:buildConfig.useNativeRot4d,useNativeMarginal:buildConfig.useNativeMarginal,prefilterVar:buildConfig.prefilterVar,sigmaScale:buildConfig.sigmaScale,renderScale:buildConfig.renderScale,canvasWidth:canvas.width,canvasHeight:canvas.height,timestamp:buildConfig.timestamp,splatScale:buildConfig.scalingModifier,elapsedMs:elapsed,stepLabel:'GPU Step25',stepNotes:['Renderer keeps packed path as the formal full-frame path','Debug timing values are normalized to avoid NaN spam','Draw fraction is now computed explicitly','Packed/sample details remain separated from generic debug extras'],tileSummary,avgRefsPerVisible,drawStats,tileSelectionText,tileDebugText,extraLines:[...extraLines,...packedLines,...sampleLines]}); setInfoText(infoEl,infoText); return {infoText,visible,packedScreenSpace,packedSummary,buildStats,drawStats,tileSummary,drawPathSummary:drawPathSelection}; }
